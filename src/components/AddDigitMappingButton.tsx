export default function AddDigitMappingButton() {
  return (
    <button
      className="toolbar-btn toolbar-btn--mapping"
      onClick={() => alert('Add Digit Mapping clicked')}
    >
      Add Digit Mapping
    </button>
  );
}
